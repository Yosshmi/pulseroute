# PulseRoute

A Go webhook delivery platform with durable PostgreSQL jobs, a bounded worker pool,
Redis rate limits, and a React operations dashboard. Synthetic data only.

Implementation and verification are tracked in [the plan](docs/implementation-plan.md).
