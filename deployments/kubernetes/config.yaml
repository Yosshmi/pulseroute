apiVersion: v1
kind: ConfigMap
metadata:
  name: pulseroute
data:
  PORT: "8080"
  WORKER_PORT: "8081"
  WORKERS: "5"
  POLL_INTERVAL: "500ms"
  HTTP_TIMEOUT: "10s"
  LEASE_DURATION: "60s"
  COOKIE_SECURE: "true"
  ALLOW_PRIVATE_ENDPOINTS: "false"
  STATIC_DIR: /app/frontend/dist
